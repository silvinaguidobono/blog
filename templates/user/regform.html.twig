{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-lg-4 box-form">
        <h2>Registro de usuarios</h2>
        {{ form_start((form)) }}
        {#
        <div class="form_row">
            {{ form_label(form.username, 'Nombre de usuario') }}
            {{ form_errors(form.username) }}
            {{ form_widget(form.username) }}
        </div>
        <div class="form_row">
            {{ form_label(form.email, 'Correo electrónico') }}
            {{ form_errors(form.email) }}
            {{ form_widget(form.email) }}
        </div>
        <div class="form_row">
            {{ form_label(form.plainpassword, 'Contraseña') }}
            {{ form_errors(form.plainpassword) }}
            {{ form_widget(form.plainpassword) }}
        </div>
        <div class="form_row">
            {{ form_label(form.plainpassword, 'Repetir contraseña') }}
            {{ form_errors(form.plainpassword) }}
            {{ form_widget(form.plainpassword) }}
        </div>
        #}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-success" value="Registrar usuario">
        {{ form_errors(form) }}
        {{ form_end(form) }}

    </div>
{% endblock %}